<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.voicecal.mapper.FestivalMapper">

    <select id="selectByMonth" resultType="com.voicecal.entity.Festival">
        SELECT * FROM festival
        WHERE (date IS NOT NULL AND MONTH(date) = #{month} AND YEAR(date) = #{year})
           OR (date IS NULL AND is_lunar = 1)
        ORDER BY type, date
    </select>

</mapper>
