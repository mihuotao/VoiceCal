# VoiceCal 语音功能模块 — 最终未完成清单

> **生成日期**: 2026-05-30
> **对照基准**: 开发方案.md + AGENTS.md
> **当前进度**: 语音基础链路已通（录音 → WebSocket → 百度ASR → NLU解析 → 前端展示）

---

## 一、核心语音命令执行（高优先级）

| #   | 功能                | 当前状态     | 说明                                                                                    |
| --- | ----------------- | -------- | ------------------------------------------------------------------------------------- |
| 1   | **语音创建事件 - 自动执行** | ⚠️ 仅返回预览 | `VoiceCommandService` 中 CREATE 意图只返回 `action.type=preview`，没有实际调用 `eventService` 创建事件 |
| 2   | **语音更新事件**        | ❌ 占位代码   | `UPDATE` 意图只有 `responseText = "好的，已为您更新事件"`，无实际逻辑                                     |
| 3   | **语音删除事件**        | ❌ 占位代码   | `DELETE` 意图只有 `responseText = "好的，已为您删除事件"`，无实际逻辑                                     |
| 4   | **语音设置提醒**        | ❌ 占位代码   | `REMINDER` 意图只有 `responseText = "好的，已为您设置提醒"`，无实际逻辑                                   |

### 详细说明

#### 1. 语音创建事件 - 自动执行

**现状**: `VoiceCommandService.processTextCommand()` 中 CREATE 分支只调用 `buildCreateRequest()` 构建预览数据，返回 `action.type=preview`，前端收到后打开 EventForm 让用户手动确认提交。

**目标**: 语音识别后自动创建事件，无需用户手动确认（或提供可配置的自动/手动模式）。

**涉及文件**:

- `backend/src/main/java/com/voicecal/voice/VoiceCommandService.java` (第76-80行)
- `frontend/src/composables/useVoice.ts` (第234-253行)

#### 2. 语音更新事件

**现状**: `VoiceNluService.matchUpdate()` 能识别"修改"、"更改"等关键词并提取标题和日期，但 `VoiceCommandService` 中 UPDATE 分支无任何执行逻辑。

**目标**: 根据标题+日期匹配事件 → 更新时间/标题等字段 → 返回更新结果。

**涉及文件**:

- `backend/src/main/java/com/voicecal/voice/VoiceCommandService.java` (第90-92行)
- `backend/src/main/java/com/voicecal/voice/VoiceNluService.java` (第106-127行)

#### 3. 语音删除事件

**现状**: `VoiceNluService.matchDelete()` 能识别"删除"、"取消"等关键词并提取标题，但 `VoiceCommandService` 中 DELETE 分支无任何执行逻辑。

**目标**: 根据标题+日期匹配事件 → 执行删除 → 返回删除结果。

**涉及文件**:

- `backend/src/main/java/com/voicecal/voice/VoiceCommandService.java` (第93-95行)
- `backend/src/main/java/com/voicecal/voice/VoiceNluService.java` (第129-149行)

#### 4. 语音设置提醒

**现状**: `VoiceNluService.matchReminder()` 能识别"提醒"、"记得"等关键词并提取标题和时间，但 `VoiceCommandService` 中 REMINDER 分支无任何执行逻辑。

**目标**: 创建带提醒的事件，或为已有事件设置提醒 → 调用 `ReminderService` 保存提醒。

**涉及文件**:

- `backend/src/main/java/com/voicecal/voice/VoiceCommandService.java` (第96-98行)
- `backend/src/main/java/com/voicecal/service/ReminderService.java`

---

## 二、NLU 意图解析增强（中优先级）

| #   | 功能                | 当前状态          | 说明                                            |
| --- | ----------------- | ------------- | --------------------------------------------- |
| 5   | **LLM 引擎集成**      | ❌ 未实现         | 开发方案要求"LLM 解析（主力）+ 规则引擎（兜底）"，目前只有规则引擎         |
| 6   | **交互澄清流程**        | ⚠️ 后端返回但前端未处理 | NLU 返回 `requiresClarify=true` 时，前端没有追问用户的交互逻辑 |
| 7   | **UPDATE 意图实体提取** | ⚠️ 不完整        | 缺少"新时间"的提取（如"把会议改到下午3点"）                      |
| 8   | **DELETE 意图事件匹配** | ❌ 未实现         | 只提取了标题，没有按标题+日期匹配具体事件并删除                      |

### 详细说明

#### 5. LLM 引擎集成

**现状**: `VoiceNluService` 是纯规则引擎（正则匹配），无 LLM 调用。

**目标**: 接入 LLM API（如百度文心一言/OpenAI），实现：

- 规则引擎无法匹配时，调用 LLM 解析意图
- LLM 返回结构化 JSON（intent + entities）
- 规则引擎作为兜底（LLM 超时/失败时回退）

**涉及文件**:

- `backend/src/main/java/com/voicecal/voice/VoiceNluService.java`
- 新增: `backend/src/main/java/com/voicecal/voice/LlmNluService.java`

#### 6. 交互澄清流程

**现状**: NLU 返回 `requiresClarify=true` 和 `clarifyQuestion`，但前端 `useVoice.ts` 收到 `final_result` 后直接进入 `result` 状态，没有展示澄清问题并等待用户补充输入。

**目标**: 

- 前端展示澄清问题（如"请问在哪一天？"）
- 用户补充语音/文字输入
- 合并上下文后重新解析

**涉及文件**:

- `frontend/src/composables/useVoice.ts`
- `frontend/src/components/voice/VoiceOverlay.vue`

#### 7. UPDATE 意图实体提取

**现状**: `matchUpdate()` 只提取标题和日期，缺少新时间提取。

**目标**: 支持以下表达：

- "把明天的会议改到下午3点" → 提取新 startTime
- "推迟会议到下周三" → 提取新 date
- "会议提前一小时" → 计算新 startTime

**涉及文件**:

- `backend/src/main/java/com/voicecal/voice/VoiceNluService.java` (第106-127行)

#### 8. DELETE 意图事件匹配

**现状**: 只提取标题字符串，没有查询数据库匹配具体事件。

**目标**: 

- 按 userId + title + date 查询匹配事件
- 多个匹配时返回列表让用户选择
- 单个匹配时直接删除

**涉及文件**:

- `backend/src/main/java/com/voicecal/voice/VoiceCommandService.java`
- `backend/src/main/java/com/voicecal/service/EventService.java`

---

## 三、语音流程闭环（中优先级）

| #   | 功能           | 当前状态     | 说明                                        |
| --- | ------------ | -------- | ----------------------------------------- |
| 9   | **冲突检测集成**   | ❌ 未集成    | 语音创建事件时，没有调用 `EventService` 检测时间冲突        |
| 10  | **节日关怀自动触发** | ⚠️ 未自动触发 | 语音创建事件时，没有检测是否为节日并自动触发祝福+TTS              |
| 11  | **日历占位块预览**  | ❌ 未实现    | 开发方案要求"语音播报 + 日历占位块 + 确认按钮"三重反馈，目前只有语音+按钮 |
| 12  | **语音查询日历定位** | ❌ 未实现    | 查询结果返回后，没有自动定位日历到对应日期                     |

### 详细说明

#### 9. 冲突检测集成

**现状**: `EventService` 有冲突检测方法，但 `VoiceCommandService` 创建事件前未调用。

**目标**: 

- 创建前调用冲突检测
- 有冲突时返回冲突信息，让用户确认是否继续
- 无冲突时直接创建

**涉及文件**:

- `backend/src/main/java/com/voicecal/voice/VoiceCommandService.java`
- `backend/src/main/java/com/voicecal/service/EventService.java`

#### 10. 节日关怀自动触发

**现状**: `FestivalController` 和 `VoiceCommandController` 有 `/festival-greeting` 接口，但语音创建事件时未自动检测节日。

**目标**: 

- 创建事件时判断日期是否为节日
- 首次在该节日创建事件时，自动触发祝福 + TTS 播报
- 记录 `festival_greeting_log` 防止重复祝福

**涉及文件**:

- `backend/src/main/java/com/voicecal/voice/VoiceCommandService.java`
- `backend/src/main/java/com/voicecal/service/FestivalService.java`

#### 11. 日历占位块预览

**现状**: 语音创建事件后，只在 VoiceOverlay 显示文本结果，日历视图无变化。

**目标**: 

- 语音识别出事件后，在日历对应日期显示半透明占位块
- 用户点击确认后，占位块变为正式事件
- 用户点击取消时，移除占位块

**涉及文件**:

- `frontend/src/components/calendar/CalendarGrid.vue`
- `frontend/src/components/calendar/CalendarCell.vue`
- `frontend/src/composables/useCalendar.ts`

#### 12. 语音查询日历定位

**现状**: 查询意图返回事件列表，但日历视图不跳转。

**目标**: 

- 查询"今天有什么安排" → 日历自动定位到今天
- 查询"下周三的会议" → 日历自动定位到下周三
- 高亮显示匹配的事件

**涉及文件**:

- `frontend/src/composables/useCalendar.ts`
- `frontend/src/composables/useVoice.ts`

---

## 四、WebSocket 实时推送（低优先级）

| #   | 功能                     | 当前状态  | 说明                  |
| --- | ---------------------- | ----- | ------------------- |
| 13  | **`/ws/calendar` 端点**  | ❌ 未实现 | 开发方案规划的事件变更推送端点     |
| 14  | **提醒实时推送**             | ❌ 未实现 | 通过 WebSocket 推送提醒通知 |
| 15  | **`EVENT_CHANGED` 消息** | ❌ 未实现 | 事件创建/更新/删除后推送通知     |
| 16  | **`REMINDER` 消息**      | ❌ 未实现 | 到达提醒时间时推送           |

### 详细说明

#### 13. `/ws/calendar` 端点

**现状**: 只有 `/ws/voice` 端点用于语音音频流传输。

**目标**: 新增 `/ws/calendar` WebSocket 端点，用于：

- 事件变更推送（创建/更新/删除）
- 提醒通知推送
- 节日祝福推送

**涉及文件**:

- `backend/src/main/java/com/voicecal/config/WebSocketConfig.java`
- 新增: `backend/src/main/java/com/voicecal/websocket/CalendarWebSocketHandler.java`

#### 14-16. 实时推送消息

**目标消息类型**:

```json
// 事件变更
{"type": "EVENT_CHANGED", "action": "CREATE|UPDATE|DELETE", "event": {...}}

// 提醒通知
{"type": "REMINDER", "eventId": 1, "title": "项目评审", "startTime": "..."}

// 节日祝福
{"type": "FESTIVAL_GREETING", "festival": "母亲节", "greeting": "..."}
```

**涉及文件**:

- `backend/src/main/java/com/voicecal/service/EventService.java`
- `backend/src/main/java/com/voicecal/scheduled/ReminderScanner.java`
- `backend/src/main/java/com/voicecal/scheduled/FestivalGreetingPushTask.java`

---

## 五、TTS 增强（低优先级）

| #   | 功能               | 当前状态    | 说明                                             |
| --- | ---------------- | ------- | ---------------------------------------------- |
| 17  | **百度云 TTS 前端调用** | ⚠️ 仅后端有 | `BaiduTtsService` 只在节日关怀接口使用，语音命令结果没有自动 TTS 播报 |
| 18  | **TTS 播报语音命令结果** | ❌ 未实现   | 创建/更新/删除成功后，没有自动 TTS 播报确认                      |

### 详细说明

#### 17. 百度云 TTS 前端调用

**现状**: `useTts.ts` 使用 Web Speech API（浏览器内置），`BaiduTtsService` 仅在 `/festival-greeting` 接口使用。

**目标**: 

- 提供 TTS 引擎切换选项（Web Speech API / 百度云 TTS）
- 百度云 TTS 音质更好，作为高级选项

**涉及文件**:

- `frontend/src/composables/useTts.ts`
- `frontend/src/composables/usePreferences.ts`

#### 18. TTS 播报语音命令结果

**现状**: 语音命令执行后，只在界面显示文本结果，无语音播报。

**目标**: 

- 创建成功 → TTS 播报"已为您创建事件「xxx」，明天下午3点到4点"
- 查询结果 → TTS 播报"今天有3个安排：9点开会，12点午餐..."
- 删除成功 → TTS 播报"已删除事件「xxx」"

**涉及文件**:

- `frontend/src/composables/useVoice.ts`
- `frontend/src/components/voice/VoiceOverlay.vue`

---

## 六、建议实现顺序

```
┌─────────────────────────────────────────────────────────────┐
│  第一阶段：核心闭环（让语音命令真正执行）                          │
│  1 → 2 → 3 → 4                                              │
│  语音创建/更新/删除/提醒 → 实际执行                              │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│  第二阶段：体验优化（交互澄清 + 智能检测）                        │
│  6 → 9 → 10 → 12                                            │
│  交互澄清 → 冲突检测 → 节日关怀 → 日历定位                       │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│  第三阶段：完整体验（TTS播报 + 确认闭环）                        │
│  17 → 18 → 11                                                │
│  TTS引擎选择 → 结果播报 → 日历占位块                            │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│  第四阶段：实时推送（WebSocket增强）                              │
│  13 → 14 → 15 → 16                                          │
│  /ws/calendar → 事件推送 → 提醒推送 → 节日推送                   │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│  第五阶段：NLU增强（LLM集成）                                    │
│  5 → 7 → 8                                                   │
│  LLM引擎 → UPDATE增强 → DELETE匹配                            │
└─────────────────────────────────────────────────────────────┘
```

---

## 七、相关文件索引

### 后端核心文件

| 文件路径                                                                        | 说明           |
| --------------------------------------------------------------------------- | ------------ |
| `backend/src/main/java/com/voicecal/voice/VoiceCommandService.java`         | 语音命令主服务      |
| `backend/src/main/java/com/voicecal/voice/VoiceNluService.java`             | NLU意图解析      |
| `backend/src/main/java/com/voicecal/voice/NluResult.java`                   | NLU结果数据类     |
| `backend/src/main/java/com/voicecal/voice/VoiceWebSocketHandler.java`       | WebSocket处理器 |
| `backend/src/main/java/com/voicecal/voice/BaiduStreamAsrService.java`       | 百度流式ASR      |
| `backend/src/main/java/com/voicecal/voice/BaiduTtsService.java`             | 百度TTS        |
| `backend/src/main/java/com/voicecal/controller/VoiceCommandController.java` | 语音命令控制器      |
| `backend/src/main/java/com/voicecal/service/EventService.java`              | 事件服务         |
| `backend/src/main/java/com/voicecal/service/ReminderService.java`           | 提醒服务         |
| `backend/src/main/java/com/voicecal/service/FestivalService.java`           | 节日服务         |

### 前端核心文件

| 文件路径                                                 | 说明          |
| ---------------------------------------------------- | ----------- |
| `frontend/src/composables/useVoice.ts`               | 语音识别组合式函数   |
| `frontend/src/composables/useWebSocket.ts`           | WebSocket管理 |
| `frontend/src/composables/useTts.ts`                 | TTS语音播报     |
| `frontend/src/composables/useCalendar.ts`            | 日历逻辑        |
| `frontend/src/composables/useEvents.ts`              | 事件CRUD      |
| `frontend/src/composables/useFestival.ts`            | 节日检测        |
| `frontend/src/components/voice/VoiceOverlay.vue`     | 语音覆盖层       |
| `frontend/src/components/voice/VoiceResult.vue`      | 识别结果展示      |
| `frontend/src/components/voice/QueryResultPanel.vue` | 查询结果面板      |
| `frontend/src/components/calendar/CalendarGrid.vue`  | 日历网格        |

---

**文档版本**: v1.0
**生成工具**: Claude Code
